<header class="">
  <nav class="mx-auto flex max-w-7xl items-center justify-between p-4" aria-label="Global">
    <div class="flex lg:flex-1">

    </div>

    <div class="hidden lg:flex lg:flex-1 lg:justify-end">
      <% if user_signed_in? %>
        <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "text-sm font-semibold leading-6 text-gray-900" %>
      <% else %>
        <%= link_to "login", new_user_session_path, class: "text-sm font-semibold leading-6 text-gray-900" %>

      <% end %>
    </div>
  </nav>
</header>
